[pytest]
asyncio_mode = auto
testpaths = tests
pythonpath = .
markers =
    integration: hits real external services (osmnx/Overpass API, and a running C++ router); excluded by default, run explicitly with `pytest -m integration`
addopts = -m "not integration"
